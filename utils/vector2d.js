/**
 * An arbitrary pair of an x-component and a y-component.
 * @constructor
 * @param {number} x - The vector's x-component.
 * @param {number} y - The vector's y-component.
 */
function Vector2D(x, y) {
  this.x = x;
  this.y = y;
}
