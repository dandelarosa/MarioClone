function Rect2D(x, y, width, height) {
  this.x = parseFloat(x); // Defensive Copy
  this.y = parseFloat(y); // Defensive Copy
  this.width = parseFloat(width); // Defensive Copy
  this.height = parseFloat(height); // Defensive Copy
}
